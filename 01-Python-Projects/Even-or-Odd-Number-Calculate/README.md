# Even or Odd Number Checker

A simple Python program that checks if a number is even or odd.

## How It Works

The program asks the user for a number and uses the modulo operator (`%`) to check if it's divisible by 2:
- If remainder is 0 → Even number
- If remainder is 1 → Odd number
